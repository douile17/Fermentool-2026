Build output of `ui/` (via `npm run build`) lands here and is embedded into the
daemon binary with rust-embed (milestone 8). The contents are git-ignored; this
file only keeps the directory present so the embed macro has a path to read.
